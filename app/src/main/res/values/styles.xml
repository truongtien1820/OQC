<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <!--item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item-->
        <item name="android:spinnerItemStyle">@style/SpinnerItem</item>
        <item name="popupMenuStyle">@style/MyPopupMenu</item>

    </style>

    <style name="MyPopupMenu" parent="Widget.AppCompat.PopupMenu.Overflow">
        <item name="android:popupBackground">@color/white</item>
    </style>

    <style name="SpinnerItem" parent="Widget.AppCompat.Light.DropDownItem.Spinner">
        <item name="android:padding">0dp</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:minHeight">0dp</item> <!-- Giảm chiều cao tối thiểu -->
        <item name="android:layout_width">match_parent</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Subhead</item> <!-- Thiết lập kiểu chữ -->
    </style>

    <style name="lv_style">
        <item name="android:textColor">#000000</item>
        <item name="android:layout_margin">5dp</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">20sp</item>
        <item name="android:padding">3dp</item>
    </style>

    <style name="TransparentDialog" parent="Theme.AppCompat.Dialog.Alert">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="btn_text_style" parent="Widget.AppCompat.Button">
        <item name="android:textColor">@color/green</item>
        <item name="android:gravity">right</item>
        <item name="android:textSize">16sp</item>
        <item name="android:padding">3dp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="title1">
        <item name="android:textColor">#F27E60</item>
        <!--        <item name="android:background">@drawable/boder_title_view1</item>-->
        <item name="android:gravity">center</item>
        <item name="android:textSize">16sp</item>
        <item name="android:padding">3dp</item>
    </style>

</resources>